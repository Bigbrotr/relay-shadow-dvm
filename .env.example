# =================================
# Relay Shadow DVM Configuration
# =================================

# Database Configuration (BigBrotr instance)
DB_HOST=your-bigbrotr-host.com
DB_NAME=bigbrotr
DB_USER=your-username
DB_PASSWORD=your-password
DB_PORT=5432

# DVM Configuration  
DVM_PRIVATE_KEY=your-nostr-private-key-hex
DVM_RELAYS=wss://relay.damus.io,wss://relay.snort.social,wss://nos.lol

# Optional Configuration
LOG_LEVEL=info
PORT=3001

# Development
NODE_ENV=production